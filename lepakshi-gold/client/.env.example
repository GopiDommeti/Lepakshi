# The client talks to the API through Vite's proxy in development,
# so there is nothing required here.
#
# Only set this if you host the API on a different domain in production:
# VITE_API_URL=https://api.yourdomain.com
